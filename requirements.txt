# Local inference on Apple Silicon (M-series).
# KenKennyfr/qwen-finetuned is a LoRA adapter -> load base + adapter via PEFT.
torch==2.5.1
transformers>=4.51.0,<5.0.0   # Qwen3 support; <5 keeps sentence-transformers happy
accelerate>=1.2.0
peft>=0.13.0
huggingface-hub>=0.34.0       # required by recent transformers
gradio
# Retrieval stack
langchain==0.3.7
langchain-community==0.3.5
langchain-huggingface==0.1.2
langchain-text-splitters==0.3.2
faiss-cpu==1.9.0
sentence-transformers==3.2.1

# Document loaders (PDF + plain text only)
pypdf==5.1.0

# Misc
python-dotenv==1.0.1
